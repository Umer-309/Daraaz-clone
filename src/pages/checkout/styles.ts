import { Box, styled } from "@mui/material";

export const Wrapper = styled(Box)(({theme}) => ({
        maxWidth: '500px',
        background: '#fff',
        margin: 'auto',
        // marginRight: 'auto',
        paddingTop: '50px',
        paddingBottom: '50px',
}))